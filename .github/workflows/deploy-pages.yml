name: Deploy front-end to GitHub Pages

on:
  push:
    branches: [main]
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: pages
  cancel-in-progress: true

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: npm
          cache-dependency-path: DevFlow.Web/package-lock.json

      - name: Install dependencies
        working-directory: DevFlow.Web
        run: npm ci

      - name: Build
        working-directory: DevFlow.Web
        run: npx ng build --base-href "/${{ github.event.repository.name }}/"

      - name: Add SPA fallback (404.html)
        run: cp DevFlow.Web/dist/DevFlow.Web/browser/index.html DevFlow.Web/dist/DevFlow.Web/browser/404.html

      - uses: actions/upload-pages-artifact@v3
        with:
          path: DevFlow.Web/dist/DevFlow.Web/browser

  deploy:
    needs: build
    runs-on: ubuntu-latest
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    steps:
      - id: deployment
        uses: actions/deploy-pages@v4
